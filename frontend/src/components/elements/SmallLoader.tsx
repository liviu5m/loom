import React from "react";

const SmallLoader = () => {
  return (
    <div className="flex items-center justify-center w-full h-full">
      <div className="w-30 h-30 border-4 border-t-loom-gold border-gray-300 rounded-full animate-spin"></div>
    </div>
  );
};

export default SmallLoader;
